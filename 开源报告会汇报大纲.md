# MyGo 开源报告会汇报大纲

> **项目名称**：MyGo —— 基于 ROS 2 的全自主移动操作机器人  
> **报告日期**：2026年5月  
> **作者**：MyGo 团队视觉负责人 Harekasa

---

## 一、项目概述

### 1.1 项目背景与目标

- 面向机器人竞赛场景（RoboMaster / 类似赛事）的半自主移动操作平台
- 实现底盘运动、五轴机械臂抓取、能量机关收集与提交等复杂任务
- 通过手柄遥控，完成任务执行

### 1.2 系统概览

- **履带式差分驱动底盘** + 5-DOF 总线舵机机械臂 + 双旋杆收集机构 + 可抬升小球收集装置
- 基于 ROS 2 Jazzy 分布式架构，模块化实现高解耦、高集成设计，采用类《终末地》派状 UI 进行操控
- 真机部署：Raspberry Pi 4B（上位机）+ STM32F407ZET6（底盘驱动）+ STM32F103C8T6（机械臂/车载机构驱动）
- 驱动板均由队长绘制，采用 **USB 模拟串口** 连接，大幅提升连接与通信稳定性
- 上位机 ROS 2 配置**开机自启**，通过串口绑定 STM32 芯片编号实现乱序连接自动识别与功能分配
- PC 端通过局域网运行 UI 界面，实现可视化控制与状态监测
- 支持 rviz 仿真与 MoveIt2 运动规划（注：整车模型面数过高，树莓派加载需 1-2 分钟，最终仅保留机械臂部分）

---

## 二、系统架构设计

### 2.1 总体架构图

```
┌──────────────────────────────────────────────────────┐
│                   操作手柄 (Xbox)                      │
└─────────────────────┬────────────────────────────────┘
                      │ /joy
                      ▼
┌──────────────────────────────────────────────────────┐
│              joystick_parser (输入抽象层)              │
│   原始摇杆 → Button/Joystick/Combo/Trigger Intent     │
└─────────────────────┬────────────────────────────────┘
                      │
                      ▼
┌──────────────────────────────────────────────────────┐
│              state_machine (核心决策层)                │
│  ┌──────┐ ┌───────┐ ┌─────┐ ┌──────┐ ┌────────────┐ │
│  │ IDLE │ │CHASSIS│ │ ARM │ │ MENU │ │VISION_TASK │ │
│  ├──────┤ ├───────┤ ├─────┤ ├──────┤ ├────────────┤ │
│  │ POLE │ │ EMERG │ │ BALL│ │  ... │ │    ...     │ │
│  └──────┘ └───────┘ └─────┘ └──────┘ └──────┘──────┘ │
└─────┬───────────────┬───────────────┬───────┬────────┘
      │               │               │       │
      ▼               ▼               ▼       ▼ 视觉目标/机械臂角度
┌──────────┐  ┌──────────────┐  ┌──────────────┐  ┌──────────────────┐
│ /cmd/    │  │ /cmd/arm/    │  │ /cmd/gripper │  │  视觉模块 (Mygo)   │
│ chassis  │  │ pose_target  │  │ /command     │  │ ┌──────────────┐ │
│ /velocity│  │ joint_target │  │              │  │ │ 仿真层        │ │
└────┬─────┘  │ named_target │  └──────┬───────┘  │ │ TargetSim    │ │
     │        └──────┬───────┘         │          │ └──────┬───────┘ │
     ▼               ▼                 ▼          │        ▼         │
┌──────────────────────────────────────────────────┐ │ 识别控制层      │
│        robot_hardware_interfaces (硬件驱动层)      │ │ TargetTracker  │
│  chassis_serial │ arm_serial │ pole_serial │ ... │ │ TargetPipeline │
└─────────────────────┬────────────────────────────┘ │ PID+遗传算法   │
                      │ USB模拟串口                    └────────┬─────────┘
                      ▼                                       │
┌──────────────────────────────────────────────────────────────┴──┐
│   STM32 嵌入式下位机执行层 (USB模拟串口)  +  MaixCam2 视觉硬件    │
│  F407(底盘) │ F103(机械臂/车载) │ 舵机驱动 │ 电机驱动 │ TCP通信    │
└─────────────────────────────────────────────────────────────────┘
```

### 2.2 设计理念

- **分层解耦**：输入抽象层 → 决策层 → 硬件驱动层 → 嵌入式执行层，每层独立可测
- **状态驱动**：有限状态机（FSM）管理 8 大模式 + 14+ 子模式，状态间互斥、转换有序。采用类《终末地》派状 UI 作为模式切换入口，右肩键（RB）切换预设位置
- **安全优先**：IDLE 防误触、EMERGENCY 急停保护、状态转换合法性校验
- **双模控制**：手柄实时操控 + 预设轨迹自动执行（MoveIt2 路径规划），灵活切换
- **仿真驱动开发**：视觉算法在 PC 端 OpenCV 仿真环境中充分验证后，通过 Sipeed MaixCDK 交叉编译迁移至 MaixCam2 嵌入式平台
- **视觉-控制闭环**：视觉模块独立于 ROS 2 主控系统运行，通过 **TCP 网络协议** 经树莓派中转反馈目标位姿，形成感知-决策-执行完整闭环
- **USB 模拟串口**：下位机驱动板实现 USB 模拟串口，采用 USB 线连接，替代传统杜邦线串口，大幅提升连接和通信稳定性
- **开机自启 + 自动识别**：ROS 2 配置开机自启，通过串口绑定 STM32 芯片编号，实现下位机乱序连接的自动识别和功能分配

---

## 三、功能模块详解

### 3.1 输入抽象层 —— `joystick_parser`

| 功能 | 说明 |
|------|------|
| 摇杆抽象 | 左/右摇杆、D-Pad → `JoystickIntent`（含死区检测、NORMAL/CENTERED/EDGE 状态） |
| 按钮抽象 | A/B/X/Y/LB/RB 等 → `ButtonIntent`（含按下/释放事件） |
| 组合键抽象 | 多按钮同时按下 → `ComboIntent`（如双扳机折叠手臂） |
| 扳机抽象 | LT/RT 模拟量 → `TriggerIntent`（0~1 线性值） |
| 速率过滤 | 摇杆轴值平滑滤波，抑制抖动 |

### 3.2 核心决策层 —— `state_machine`

#### 3.2.1 状态机总览（8 大状态）

| 状态 | 编号 | 功能简述 |
|------|------|----------|
| **IDLE** | 1 | 启动默认状态，仅可进入 MENU，防误触 |
| **CHASSIS** | 2 | 底盘模式，含 8 个子模式（详见下） |
| **ARM** | 3 | 机械臂模式：摇杆控制 XYZ+Yaw+Pitch，扳机控制夹爪，组合键预设动作 |
| **MENU** | 4 | 菜单选择：右摇杆导航 + 松扳机确认 |
| **POLE** | 5 | 回收杆模式：扳机模拟量控制旋转 |
| **VISION_TASK** | 7 | 视觉任务模式：类比 ARM 操控 + 视觉锁定辅助 |
| **EMERGENCY** | 8 | 急停模式：立即刹停所有执行器 |

#### 3.2.2 CHASSIS 子模式（8 个）

| 子模式 | 功能 |
|--------|------|
| HOME | 底盘起始状态，双扳机折叠手臂至发车姿态 |
| NORMAL_DETECTION | YOLO + 单目深度预测，检测方块/能量单元，反馈至 UI |
| CYLINDER_SUBMISSION | 能量单元抓取 → 识别洞口 → 放置提交 |
| CUBE_SUBMISSION | 方块抓取 → 识别投放口 → 放置提交 |
| CYLINDER_COLLECTION | 能量单元抓取 → 收纳至储存仓 |
| CUBE_COLLECTION | 方块抓取 → 收纳至储存仓 |
| UNDER_BRIDGE | 预设动作通过桥洞障碍 |
| BALL_SUBMISSION | 球类物品连续提交序列 |

#### 3.2.3 状态转换规则

- IDLE → MENU（左扳机进入菜单）
- MENU → {CHASSIS, ARM, EMERGENCY, VISION_TASK, IDLE, POLE}
- CHASSIS 内子模式通过 LB 按钮 + 右摇杆切换，均以 HOME 为中转
- EMERGENCY 退出后恢复进入前的状态

### 3.3 硬件驱动层 —— `robot_hardware_interfaces`

| 节点 | 订阅话题 | 功能 |
|------|----------|------|
| `chassis_serial_node` | `/cmd/chassis/velocity` (Twist) | 差分驱动底盘控制（linear.x + angular.z → 左右轮速） |
| `arm_serial_node` | `/cmd/arm/pose_target`, `/cmd/arm/joint_target`, `/cmd/arm/named_target` | MoveIt2 规划 → 手动解析路径信息 → 转化为当前时刻舵机角度 → USB串口下发（注：未使用 ros2_control，手动封装硬件接口） |
| `pole_serial_node` | `/cmd/pole/rotate` | 回收杆旋转控制（扳机模拟量映射） |
| `collector_serial_node` | （字符串指令） | 双旋杆收集机构控制 |
| `camera_serial_node` | — | 相机硬件接口 |
| `vision_tcp_bridge_node` | — | 视觉系统 TCP 通信桥接（树莓派中转 → 下位机控制板） |

**串口抽象基类**：`SendCommand` 类封装 Linux USB模拟串口通信（termios），统一数据帧格式与收发逻辑。底盘下发形如 `#0.23,0.33` 的差速指令。

### 3.4 机器人模型 —— `mygo_robot_description`

- **机械臂**：5-DOF 连续旋转关节（joint1~5），STL 网格碰撞模型
- **夹爪**：2-DOF 对称 mimic 关节（end_joint2 = -end_joint1）
- **底盘**：差分驱动轮式结构
- **材质**：gray / dark_gray / white 三色视觉材质

### 3.5 运动规划 —— `mygo_moveit_config`

- IK 求解器：KDL / LMA（`kinematics.yaml` 可配置）
- **实际实现**：MoveIt2 完成运动学逆解算与轨迹规划，但**未使用 ros2_control** 的 JointTrajectoryController。改为手动封装机械臂硬件接口，直接解析 MoveIt 输出的路径信息 → 转化为当前时刻舵机角度 → 打包下发至 USB 串口
- 碰撞检测：`mygo.srdf` 禁用自碰撞关节对，避免自干涉
- **已知限制**：总线舵机内部自带角度闭环，但无法实现系统级闭环控制；整车 URDF 模型面数过高，树莓派加载需 1-2 分钟，最终仅保留机械臂部分 + 必要底座，无法进行整车仿真，操作手需确保机械臂从安全位置运动以避免实车干涉

### 3.6 系统启动 —— `mygo_bringup`

- `bringup.launch.xml`：核心节点启动（joystick_parser + state_machine + 基础串口）
- `bringup_on_raspi.launch.xml`：全系统启动（含 robot_state_publisher + ros2_control + MoveIt + 全部串口节点）
- `moveit_test_bringup.launch.xml`：MoveIt 专项测试

### 3.7 自定义接口 —— `custom_interfaces`

共定义 **11 种消息类型 + 1 种服务**：

| 接口 | 类型 | 用途 |
|------|------|------|
| `RobotState` | Msg | 机器人全局状态报告 |
| `JoystickIntent` | Msg | 摇杆抽象输入（含死区状态） |
| `ButtonIntent` | Msg | 按键按下/释放事件 |
| `ComboIntent` | Msg | 多按钮组合 |
| `TriggerIntent` | Msg | 扳机模拟量 |
| `ArmPoseTarget` | Msg | 笛卡尔空间目标位姿 |
| `ArmJointTarget` | Msg | 关节空间目标角度 |
| `ArmNamedTarget` | Msg | 命名预设位姿 |
| `GripperCommand` | Msg | 夹爪开/合指令 |
| `PoleCommand` | Msg | 回收杆旋转指令 |
| `MenuNavigation` | Msg | 菜单导航状态 |
| `SetMode` | Srv | 外部模式切换服务 |

### 3.8 视觉模块 —— `Mygo/` 独立视觉项目

> 视觉模块是与 ROS 2 主控系统解耦的独立 C++ 项目，负责目标色块识别、3D 仿真验证与云台控制。PC 端基于 OpenCV 完成完整仿真测试后，使用 Sipeed MaixCDK 交叉编译至 MaixCam2 嵌入式平台部署，通过 **TCP 网络协议** 经树莓派中转与主控系统通信。
>
> **实际部署调整**：由于车上空间限制，原计划的独立云台被取消，相机直接固定在机械臂上部 pitch 轴处（通过自行设计的 3D 打印件连接），导致外参难以精确测定，且机械臂运动带来控制滞后问题。

#### 3.8.1 视觉模块总体架构

项目采用 **仿真层 → 识别控制层 → 应用层** 三层架构，仿真层与识别控制层编译为独立库，仅通过 `cv::Mat` 接口传递数据：

| 层级 | 模块 | 核心职责 |
|------|------|----------|
| **仿真层** (`lib target_sim`) | `TargetSim` / `TrainingFrameGenerator` / `SimulationCamera` / `PentagonSimulator` / `Target3DGenerator` | 靶子几何建模 → 动态运动生成 → 3D 相机投影 → 仿真图像输出 |
| **识别控制层** (`lib target_tracker`) | `TargetTracker` / `TargetTrackingPipeline` / `GimbalControl` / `GeneticAlgorithm` / `SerialPort` | 多色彩空间色块识别 → 状态机追踪 → 双轴 PID 云台控制 → 遗传算法调参 → 串口舵机指令下发 |
| **应用层** (`apps`) | 8 个可执行目标 | 仿真演示 / 实机控制 / 遗传算法训练 / 单元测试 |

#### 3.8.2 仿真层 — 靶子建模与 3D 投影

**靶子建模 (`TargetSim`)**：
- 1 个中心色块 + 5 个外围色块组成五角星布局（12 种预定义 BGR 颜色）
- 基于五角星顶点坐标精确计算外围色块位置，支持任意旋转角度
- 对外暴露 Ground Truth（`get_blob_centroids()`），便于识别算法精度评估

**流式帧生成 (`TrainingFrameGenerator`)**：
- 4 种运动模式：五角星旋转、直线运动、随机位置出现、参数方程运动（圆周/正弦/李萨如）
- 支持 `std::function<float(float t)>` 角速度函数注入，模拟真实物理模型
- 内置帧率精确控制，仿真帧与真实时间对齐

**3D 仿真相机 (`SimulationCamera`)**：
- 针孔内参模型（$f_x, f_y, c_x, c_y$）+ 畸变系数（$k_1, k_2, p_1, p_2$）
- 6-DOF 外参（位置 + 欧拉角姿态），完整世界坐标 → 图像坐标变换链：`world_to_image()`
- 传感器尺寸参数（`sensor_width/height`）用于毫米级精度建模

**顶层仿真封装 (`PentagonSimulator`)**：
- 整合靶子生成 + 相机投影，提供一站式仿真接口
- 能量机关角速度模拟：$a \cdot \sin(\omega t) + b$ 物理模型，参数随机采样
- 双相机模型（俯视源相机 + 前方观察相机），支持运行时交互调整 pitch/yaw
- 激光红点模拟：可配置激光发射器偏移量和光斑半径

#### 3.8.3 识别算法 — 多色彩空间融合 + ROI 跟踪

**算法演进历程**：

| 版本 | 方法 | 核心改进 |
|------|------|----------|
| V1 | 全图色块两两比较距离找中心 | 基础实现 |
| V2 | 引入圆形度（Circularity）筛选 | 大幅降低比较次数，仅需 ~6 次确定中心 |
| V3 | HSV + LAB 色彩空间融合 | 解决黑色/深色/相近色误识别 |

**多色彩空间自适应融合**：
- BGR 距离 + HSV 色调 + LAB 色差三维加权评分
- 根据中心颜色亮度自适应调整权重：深色中心降低 BGR 权重、提高 LAB 权重
- Softmax 归一化提升置信度区分度，置信度比值门控（最高/次高 ≥ 1.5×）

**ROI 跟踪 + Kalman 滤波**：
- 锁定靶子后自动切 ROI 模式（仅裁剪靶子周围 ±padding 像素），大幅降低后续帧处理开销
- Kalman 滤波器预测靶子运动状态，提供运动先验
- 丢失计数器触发后退回全图搜索，保证跟踪鲁棒性

**激光红点检测**（独立检测通道）：（未使用）
- HSV 双色调范围覆盖（0°~12° + 168°~179°），适应红色环绕特性
- 形态学开运算去噪 + 膨胀增强 + 圆形度评分筛选
- 支持短时丢失保持，避免激光丢失时跳变

#### 3.8.4 控制层 — 从像素到舵机角度

**追踪流水线 (`TargetTrackingPipeline`)**：

```
Waiting → Searching（扫描模式:三角波Yaw） → Locked（连续锁定N帧） → Tracking（PID闭环）
              ↑                              ↓ 丢失超时              ↓ 丢失超时
              └──────────────────────────────┴──────────────────────┘
```

**角度解算公式**（像素偏差 → 角度偏差）：

$$\text{pitch\_error} = -\frac{y_{\text{target}} - c_y}{f_y} \quad \text{yaw\_error} = \frac{x_{\text{target}} - c_x}{f_x}$$

**追踪控制策略演进**：

| 阶段 | 策略 | 问题 |
|------|------|------|
| V1 | 像素误差 → PID 直接控制 pitch/yaw | 对识别精度和控制精度要求极高，效果不佳 |
| V2 | 靶面外参 + 相机内参单目测距 → 开环圆形轨迹跟随 + PID 像素误差修正 | 过度依赖理想条件，实际比赛环境表现不理想 |
| 最终 | 折衷方案：ROI + Kalman 预测 + PID 闭环 | 仿真效果好，实机受限于相机固定方式和环境复杂性 |

**实机追踪的已知问题**：
- 相机固定于机械臂而非独立云台，外参难以精确标定
- 机械臂运动导致控制滞后和抖动
- 实际比赛环境光照复杂，算法鲁棒性不足
- 总体而言追踪方案工程学设计不够完善，只有仿真和识别部分体现了技术含量

**双轴 PID 控制**：
- 独立 $K_p, K_i, K_d$ 参数，积分限幅（30°）、输出限幅（180°/s）
- 微分低通滤波抑制高频噪声，死区 8px 避免微抖动
- 调参经验：$K_i$ 宜小、优先降低 $K_d$ 减小超调、收紧输出步进抑制振荡

**舵机控制 (`GimbalControl` + `SerialPort`)**：
- 角度设定 → 零位修正 → PWM 映射（500-2500μs）→ 速度感知时间计算 → 协议打包（`#001P1500T0500!`）→ 串口下发
- 双映射模式：线性映射 + 零位偏移映射

#### 3.8.5 遗传算法 PID 自动整定

在仿真环境中自动搜索最优 $K_p, K_i, K_d$ 参数（**实验性功能，实际未用于比赛**）：

| 参数 | 默认值 | 说明 |
|------|--------|------|
| 种群大小 | 50 | 每代个体数 |
| 变异率 | 0.5 | 基因突变概率 |
| 变异幅度 | σ=0.3 | 高斯噪声标准差 |
| 最大代数 | 300 | 迭代次数上限 |
| 精英数 | 2 | 每代保留最优个体数 |

**适应度函数**（多轨迹综合评估）：
- 随机跳跃目标 × N + 正弦追踪 × N + 圆周追踪 × N + 李萨如轨迹 × N
- 评分维度：时间效率 $w_t$ + 跟踪误差 $w_e$ + 控制平滑度 $w_s$

**遗传算子**：`crossover`（线性混合父代参数）+ `mutate`（高斯噪声变异）+ `clamp`（参数范围限制）

**⚠️ 实际应用限制**：
- 仿真中仅需几秒即可完成的迭代，实机运行需半小时以上，效率极低
- 仿真得到的 PID 参数与实际控制过程差异大，不能直接使用
- 适应度函数编写要求极高，对突变因数、突变范围等超参数敏感
- **最终结论**：遗传算法在仿真中可作为参考，但**实机 PID 调参仍需人工迭代**，本项目最终采用传统人工调参方案

---

## 四、技术栈

### 4.1 机器人框架

| 技术 | 用途 |
|------|------|
| **ROS 2 (Jazzy)** | 分布式通信框架、节点管理、话题/服务/动作通信 |
| **rclcpp** | C++ 客户端库 |
| **ament_cmake** | 构建系统 |
| **colcon** | 工作空间编译工具 |
| **launch / launch_ros** | 多节点启动编排 |

### 4.2 运动规划与控制

| 技术 | 用途 |
|------|------|
| **MoveIt 2** | 机械臂运动规划框架（IK 求解 + 轨迹生成 + 碰撞检测） |
| **ros2_control** | 硬件控制抽象框架（ControllerManager + JointTrajectoryController） |
| **KDL / LMA** | 逆运动学求解器 |
| **SRDF** | 语义机器人描述（规划组、碰撞矩阵） |

### 4.3 仿真与可视化

| 技术 | 用途 |
|------|------|
| **RViz2** | 3D 可视化与调试 |
| **Xacro (URDF)** | 机器人模型宏定义 |
| **STL 网格** | 碰撞与视觉模型 |

### 4.4 底层通信与硬件

| 技术 | 用途 |
|------|------|
| **Linux termios (POSIX Serial)** | USB模拟串口通信（ROS ↔ STM32） |
| **STM32 嵌入式** | 下位机舵机/电机驱动、传感器采集 |
| **sensor_msgs/Joy** | 手柄原始数据接入 |
| **tf2** | 坐标系变换管理 |

### 4.5 UI 与交互

| 技术 | 用途 |
|------|------|
| **Qt5 (Widgets)** | 菜单 UI 界面渲染 |
| **Xbox 手柄** | 操作员主控输入设备 |

### 4.6 视觉算法与仿真

| 技术 | 用途 |
|------|------|
| **OpenCV 4.x** (core, imgproc, highgui, video) | 图像处理、色块识别、Kalman 滤波、透视变换、形态学操作 |
| **遗传算法** | PID 参数自动整定（种群进化 + 多轨迹适应度评估） |
| **3D 投影模型** | 针孔相机模型 + 畸变校正 + 6-DOF 外参 + 射线-平面求交 |
| **PID 控制器** | 双轴云台角度闭环控制（积分限幅 + 微分低通 + 死区） |
| **多色彩空间融合** | BGR / HSV / LAB 三维加权自适应匹配 |

### 4.7 开发与工程化

| 技术 | 用途 |
|------|------|
| **C++ 17** | 主要开发语言 |
| **Python 3** | 启动脚本、辅助工具 |
| **CMake** | 跨平台构建配置 |
| **Git / GitHub** | 版本控制与协作 |
| **VS Code** | 集成开发环境 |

---

## 五、关键技术实现原理

### 5.1 分层抽象输入处理

```
原始 /joy (sensor_msgs/Joy)
    → joystick_parser 解析为语义化 Intent
    → state_machine 根据当前状态分发 Intent
    → 各状态处理器执行对应逻辑
    → 发布硬件指令话题
```

- 死区检测：摇杆轴绝对值 < 阈值 → CENTERED，> 阈值 → EDGE
- 速率过滤：指数平滑抑制高频抖动
- 事件化：按钮连续按下只触发一次（edge trigger）

### 5.2 有限状态机 (FSM) 引擎

- 每个状态为独立 C++ 类，继承 `State` 基类
- 状态表定义合法转换集合，非法转换直接拒绝
- 状态进入/退出钩子（`onEnter` / `onExit`）处理初始化和清理
- 子模式切换通过 LB 按钮调出子菜单，松键确认

### 5.3 MoveIt2 运动规划集成

- **笛卡尔路径**：`ArmPoseTarget` → IK 求解 → 关节轨迹 → 手动解析路径信息 → 舵机角度 → USB串口下发（实际并未使用 ros2_control 的 JointTrajectoryController）
- **关节目标**：`ArmJointTarget` 直接指定关节角度
- **命名目标**：`ArmNamedTarget` 触发预设位姿（如 home、folded），通过右肩键（RB）切换
- **起始状态同步**：规划前从硬件反馈更新 MoveIt 当前状态，防止回弹
- **手动硬件接口封装**：直接解析 MoveIt 输出的轨迹点，转化为当前时刻的舵机角度并打包下发。总线舵机内部自带角度闭环，一定程度上保证角度准确性

### 5.4 串口通信协议

- Linux termios 配置（波特率、数据位、停止位、校验位）
- 统一帧格式：帧头 + 指令ID + 数据载荷 + 校验 + 帧尾
- 超时重发与 CRC 校验机制

### 5.5 安全机制

- **IDLE 防误触**：启动后仅可进入 MENU，屏蔽所有操作指令
- **EMERGENCY 急停**：立即切断所有执行器指令
- **状态转换校验**：非法跳转直接拒绝，记录日志
- **位姿快照**：调试模式下定时保存关节角度至 Markdown 文件，便于问题复现

### 5.6 多色彩空间融合识别

```
输入帧 → 色块提取(extract_color_blobs)
       → 中心色块定位(find_center_blob)
       → 目标色块匹配(find_matching_target) → BGR距离 + HSV色调 + LAB色差 三维加权
       → 激光红点检测(detect_laser_dot)
       → 输出 TargetInfo
```

- **自适应权重**：根据中心颜色亮度动态调整 BGR / HSV / LAB 权重比例，深色中心提高 LAB 色差权重
- **Softmax 归一化**：对候选相似度列表做 softmax，配合置信度比值门控（最高/次高 ≥ 1.5×）筛选最终匹配
- **ROI 加速**：锁定靶子后仅处理靶子周围 ±padding 区域，Kalman 滤波提供运动预测，丢失后退回全图搜索
- **激光双色调覆盖**：0°~12° + 168°~179° 覆盖 HSV 红色环绕特性，形态学去噪 + 圆形度评分 + 短时保持

### 5.7 3D 仿真相机投影模型

```
世界坐标 P_world → 外参变换(R|t) → 相机坐标 P_cam
    → 针孔投影(fx,fy,cx,cy) → 归一化坐标
    → 畸变模型(k1,k2,p1,p2) → 像素坐标(u,v)
```

- 完整的透视投影变换链，支持靶平面 2D 图像投影到 3D 空间
- 双相机模型（源相机 + 观察相机），通过透视变换实现视角切换
- 角速度函数注入：$a \cdot \sin(\omega t) + b$ 模拟能量机关旋转曲线

### 5.8 遗传算法 PID 整定

- **编码方案**：每个个体为 $(K_p, K_i, K_d)$ 三元组，实数编码
- **适应度评估**：在仿真中运行多条复杂轨迹（随机跳跃 + 正弦 + 圆周 + 李萨如），综合时间效率 $w_t$、跟踪误差 $w_e$、控制平滑度 $w_s$ 加权评分
- **遗传操作**：精英保留（top 2）→ 线性混合交叉 → 高斯变异（σ=0.3）→ 参数钳位
- **收敛策略**：300 代迭代 + 早停机制，最终输出最优 PID 参数直接用于实机部署

---

## 六、项目亮点与创新

### 6.1 ROS 2 主控系统

1. **完整的分层架构**：从手柄输入到嵌入式驱动的 4 层解耦设计，各层可独立替换与测试
2. **丰富的状态机设计**：8 大主状态 + 14+ 子状态，结合类《终末地》派状 UI 实现直观的模式切换
3. **双模操控融合**：实时手柄操控 + MoveIt2 预设轨迹自动执行，兼顾灵活性与自动化
4. **MoveIt2 + 手动硬件封装**：利用 MoveIt2 完成 IK 解算与轨迹规划，手动封装硬件接口解析路径下发，避免 ros2_control 的复杂配置
5. **自定义接口体系**：11 种语义化消息类型 + 1 种服务，避免直接传递原始传感器数据
6. **USB 模拟串口 + 开机自启**：USB 线替代杜邦线大幅提升稳定性，串口绑定芯片编号实现乱序自动识别

### 6.2 视觉模块

7. **仿真驱动开发**：从靶子几何建模到 3D 投影均自研实现，算法在仿真中充分验证后再交叉编译上线 MaixCam2，不依赖外部游戏引擎
8. **多色彩空间自适应识别**：BGR / HSV / LAB 三空间融合，根据中心颜色亮度自适应调整权重，结合 Softmax 归一化与圆形度筛选，有效解决深色/相近色误识别
9. **完整的 3D 投影模型**：自研针孔内参 + 畸变系数 + 6-DOF 外参相机模型，支持运行时交互调整视角
10. **遗传算法 PID 探索**：在仿真中通过多轨迹综合评估自动搜索最优 PID 参数，虽不适合直接实机部署，但为调参提供了有价值的参考方向
11. **仿真-实机无缝迁移**：核心算法纯 C++/OpenCV 实现，仿真层与识别控制层完全解耦，同一套代码 PC 仿真 → MaixCDK 交叉编译 → MaixCam2 部署

### 6.3 经验与反思

12. **仿真与现实的差距**：遗传算法在仿真中效果良好但实机不适用；PID 跟踪在仿真中稳定但实机受限于物理约束（相机固定方式、环境光照等）
13. **工程取舍**：整车 URDF 模型面数过高无法在树莓派部署，最终仅保留机械臂部分；云台因空间限制被取消，相机固连机械臂带来控制挑战

---

## 七、Q&A 预留

---

> 本报告基于 MyGo 项目源码分析生成，项目地址：（请补充仓库地址）
